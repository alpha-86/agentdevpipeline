# tech-lead 执行手册（兼容层 — 仅供遗留引用）

> **此文件已废弃。正式角色口径为 `架构师`，对应文件为 `architect.playbook.md`。**
>
> 1.0 收口后，所有新引用应使用 `architect`（对应 `architect.playbook.md`）。
> 本文件保留仅用于兼容仍在引用 `tech-lead` 的遗留路径，后续迭代中将逐步清除。

相关文件：
- `architect.playbook.md` — 架构师执行手册（正式）
