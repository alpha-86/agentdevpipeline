# 048 Phase 5.5 中文依赖接入说明

## 结论

当前仓库不需要复杂的语言运行时安装说明，真正需要的是“中文依赖接入说明”。

也就是把以下三层写清楚：

1. 仓库自身资产怎么先接入
2. 协作环境依赖怎么补齐
3. 可选宿主能力怎么按需增强

## 本轮落点

- 在 `docs/reference/dependencies.md` 中新增“中文依赖接入说明”
- 在 `plugins/agentdevpipeline/README.md` 中新增“最小接入方式”
- 在根 `README.md` 中新增“最小接入路径”
- 在执行计划中勾选 `5.5`

## 当前接入顺序

1. 先接入仓库自身资产
2. 再接入 `git`、Issue、PR 等协作依赖
3. 最后按需接入 `gh`、`gstack`、`superpowers`、CI

## 下一步

`Phase 5` 已完成，下一轮进入 `Phase 6`，继续逐机制对照 hedge-ai 做精修。
